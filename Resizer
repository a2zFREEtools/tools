<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Free online image resizer tool. Resize, compress and optimize your images for web and social media without losing quality.">
    <meta name="keywords" content="image resizer, resize images, compress images, image optimizer, online tool">
    <title>ImageResizerPro - Resize and Optimize Your Images Online</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            color: #333;
            line-height: 1.6;
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        header {
            text-align: center;
            padding: 30px 0;
            color: white;
        }

        header h1 {
            font-size: 2.8rem;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }

        header p {
            font-size: 1.2rem;
            max-width: 800px;
            margin: 0 auto;
            opacity: 0.9;
        }

        .main-content {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 30px;
        }

        .resize-tool {
            flex: 1 1 700px;
            background: white;
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
        }

        .tool-title {
            font-size: 1.8rem;
            margin-bottom: 20px;
            color: #2575fc;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .upload-area {
            border: 3px dashed #6a11cb;
            border-radius: 12px;
            padding: 40px 20px;
            text-align: center;
            margin-bottom: 25px;
            transition: all 0.3s;
            background: #f8f9ff;
            cursor: pointer;
        }

        .upload-area:hover {
            background: #eef1ff;
            border-color: #2575fc;
        }

        .upload-area i {
            font-size: 3.5rem;
            color: #6a11cb;
            margin-bottom: 15px;
        }

        .upload-area h3 {
            font-size: 1.5rem;
            margin-bottom: 10px;
            color: #333;
        }

        .upload-area p {
            color: #666;
            margin-bottom: 15px;
        }

        .upload-btn {
            background: #6a11cb;
            color: white;
            border: none;
            padding: 12px 30px;
            border-radius: 50px;
            font-size: 1rem;
            cursor: pointer;
            transition: background 0.3s;
        }

        .upload-btn:hover {
            background: #2575fc;
        }

        #file-input {
            display: none;
        }

        .resize-controls {
            margin: 25px 0;
        }

        .control-group {
            margin-bottom: 20px;
        }

        .control-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #444;
        }

        .input-group {
            display: flex;
            gap: 15px;
            flex-wrap: wrap;
        }

        .dimension-input {
            flex: 1;
            min-width: 150px;
        }

        .dimension-input input {
            width: 100%;
            padding: 12px 15px;
            border: 2px solid #ddd;
            border-radius: 8px;
            font-size: 1rem;
            transition: border-color 0.3s;
        }

        .dimension-input input:focus {
            border-color: #6a11cb;
            outline: none;
        }

        .slider-container {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .slider {
            flex: 1;
            -webkit-appearance: none;
            height: 8px;
            border-radius: 5px;
            background: #ddd;
            outline: none;
        }

        .slider::-webkit-slider-thumb {
            -webkit-appearance: none;
            width: 20px;
            height: 20px;
            border-radius: 50%;
            background: #6a11cb;
            cursor: pointer;
        }

        .quality-value {
            font-weight: bold;
            color: #6a11cb;
            min-width: 40px;
        }

        .ratio-container {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-top: 10px;
        }

        .preview-container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin: 30px 0;
        }

        .image-preview {
            flex: 1 1 300px;
            text-align: center;
        }

        .image-preview h4 {
            margin-bottom: 10px;
            color: #444;
        }

        .preview-img {
            max-width: 100%;
            max-height: 300px;
            border-radius: 8px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            border: 1px solid #eee;
        }

        .file-info {
            margin-top: 10px;
            font-size: 0.9rem;
            color: #666;
        }

        .action-buttons {
            display: flex;
            gap: 15px;
            margin-top: 20px;
        }

        .btn {
            padding: 12px 25px;
            border-radius: 50px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            border: none;
            transition: all 0.3s;
        }

        .resize-btn {
            background: #6a11cb;
            color: white;
            flex: 2;
        }

        .resize-btn:hover {
            background: #2575fc;
        }

        .resize-btn:disabled {
            background: #ccc;
            cursor: not-allowed;
        }

        .download-btn {
            background: #25d366;
            color: white;
            flex: 1;
            display: none;
        }

        .download-btn:hover {
            background: #1da851;
        }

        .ad-space {
            flex: 1 1 300px;
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .ad-unit {
            background: white;
            border-radius: 12px;
            padding: 20px;
            text-align: center;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            min-height: 280px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        .ad-unit h3 {
            color: #666;
            margin-bottom: 15px;
            font-size: 1.2rem;
        }

        .ad-placeholder {
            width: 100%;
            height: 250px;
            background: #f1f5ff;
            border-radius: 8px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            color: #6a11cb;
            font-weight: 600;
        }

        .ad-placeholder i {
            font-size: 2.5rem;
            margin-bottom: 15px;
        }

        .features {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin: 40px 0;
        }

        .feature-box {
            flex: 1 1 250px;
            background: white;
            border-radius: 12px;
            padding: 25px;
            text-align: center;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .feature-box i {
            font-size: 2.5rem;
            color: #6a11cb;
            margin-bottom: 15px;
        }

        .feature-box h3 {
            margin-bottom: 15px;
            color: #333;
        }

        .feature-box p {
            color: #666;
        }

        footer {
            text-align: center;
            padding: 20px;
            color: white;
            margin-top: 40px;
        }

        footer p {
            opacity: 0.8;
        }

        @media (max-width: 768px) {
            .main-content {
                flex-direction: column;
            }
            
            .ad-space {
                flex-direction: row;
                flex-wrap: wrap;
            }
            
            .ad-unit {
                flex: 1 1 100%;
            }
            
            header h1 {
                font-size: 2.2rem;
            }
            
            .tool-title {
                font-size: 1.5rem;
            }
        }

        @media (max-width: 480px) {
            .action-buttons {
                flex-direction: column;
            }
            
            .preview-container {
                flex-direction: column;
            }
            
            .upload-area {
                padding: 20px 10px;
            }
            
            .upload-area i {
                font-size: 2.5rem;
            }
            
            .upload-area h3 {
                font-size: 1.3rem;
            }
            
            .input-group {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>ImageResizerPro</h1>
            <p>Resize and optimize your images without sacrificing quality. Fast, easy, and completely free!</p>
        </header>

        <div class="main-content">
            <div class="resize-tool">
                <h2 class="tool-title"><i class="fas fa-expand-arrows-alt"></i> Image Resizer Tool</h2>
                
                <div class="upload-area" id="drop-area">
                    <i class="fas fa-cloud-upload-alt"></i>
                    <h3>Upload Your Image</h3>
                    <p>Drag & drop your image here or click to browse</p>
                    <button class="upload-btn">Select Image</button>
                    <input type="file" id="file-input" accept="image/*">
                </div>
                
                <div class="resize-controls">
                    <div class="control-group">
                        <label for="image-dimensions">Resize Dimensions (px)</label>
                        <div class="input-group">
                            <div class="dimension-input">
                                <input type="number" id="width-input" placeholder="Width" min="1" disabled>
                            </div>
                            <div class="dimension-input">
                                <input type="number" id="height-input" placeholder="Height" min="1" disabled>
                            </div>
                        </div>
                        <div class="ratio-container">
                            <input type="checkbox" id="maintain-ratio" checked>
                            <label for="maintain-ratio">Maintain aspect ratio</label>
                        </div>
                    </div>
                    
                    <div class="control-group">
                        <label for="compression-level">Compression Level</label>
                        <div class="slider-container">
                            <input type="range" min="0" max="100" value="85" class="slider" id="compression-level">
                            <span class="quality-value" id="quality-value">85%</span>
                        </div>
                    </div>
                </div>
                
                <div class="preview-container">
                    <div class="image-preview">
                        <h4>Original Image</h4>
                        <img id="original-preview" class="preview-img" src="https://via.placeholder.com/400x300?text=Upload+Image" alt="Original image preview">
                        <div class="file-info" id="original-info">Size: 0 KB | Dimensions: 0x0</div>
                    </div>
                    
                    <div class="image-preview">
                        <h4>Resized Image</h4>
                        <img id="resized-preview" class="preview-img" src="https://via.placeholder.com/400x300?text=Resized+Image" alt="Resized image preview">
                        <div class="file-info" id="resized-info">Size: 0 KB | Dimensions: 0x0 | Quality: 0%</div>
                    </div>
                </div>
                
                <div class="action-buttons">
                    <button class="btn resize-btn" id="resize-btn" disabled>Resize Image</button>
                    <button class="btn download-btn" id="download-btn">Download</button>
                </div>
            </div>
            
            <div class="ad-space">
                <div class="ad-unit">
                    <h3>Advertisement</h3>
                    <div class="ad-placeholder">
                        <i class="fas fa-ad"></i>
                        <p>AdSense Ad Unit</p>
                        <p>300x250</p>
                    </div>
                </div>
                
                <div class="ad-unit">
                    <h3>Advertisement</h3>
                    <div class="ad-placeholder">
                        <i class="fas fa-ad"></i>
                        <p>AdSense Ad Unit</p>
                        <p>300x600</p>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="features">
            <div class="feature-box">
                <i class="fas fa-bolt"></i>
                <h3>Fast Resizing</h3>
                <p>Resize your images in seconds with our optimized algorithm</p>
            </div>
            
            <div class="feature-box">
                <i class="fas fa-shield-alt"></i>
                <h3>Secure & Private</h3>
                <p>Your images are processed locally and never uploaded to our servers</p>
            </div>
            
            <div class="feature-box">
                <i class="fas fa-mobile-alt"></i>
                <h3>Responsive Design</h3>
                <p>Works perfectly on desktop, tablet, and mobile devices</p>
            </div>
            
            <div class="feature-box">
                <i class="fas fa-cloud-download-alt"></i>
                <h3>Easy Download</h3>
                <p>Download your resized images with a single click</p>
            </div>
        </div>
        
        <footer>
            <p>&copy; 2023 ImageResizerPro. All rights reserved.</p>
            <p>This tool is built with HTML5 Canvas and JavaScript for client-side image processing.</p>
        </footer>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Elements
            const fileInput = document.getElementById('file-input');
            const dropArea = document.getElementById('drop-area');
            const widthInput = document.getElementById('width-input');
            const heightInput = document.getElementById('height-input');
            const maintainRatio = document.getElementById('maintain-ratio');
            const compressionSlider = document.getElementById('compression-level');
            const qualityValue = document.getElementById('quality-value');
            const originalPreview = document.getElementById('original-preview');
            const resizedPreview = document.getElementById('resized-preview');
            const originalInfo = document.getElementById('original-info');
            const resizedInfo = document.getElementById('resized-info');
            const resizeBtn = document.getElementById('resize-btn');
            const downloadBtn = document.getElementById('download-btn');
            
            // Variables
            let originalImage = null;
            let resizedImageBlob = null;
            let aspectRatio = 0;
            
            // Initialize with resize button disabled
            resizeBtn.disabled = true;
            
            // Update quality value display
            compressionSlider.addEventListener('input', function() {
                qualityValue.textContent = `${this.value}%`;
            });
            
            // File selection via button
            dropArea.querySelector('.upload-btn').addEventListener('click', function() {
                fileInput.click();
            });
            
            // File input change
            fileInput.addEventListener('change', handleFileSelect);
            
            // Maintain aspect ratio logic
            widthInput.addEventListener('input', function() {
                if (maintainRatio.checked && aspectRatio > 0) {
                    heightInput.value = Math.round(widthInput.value / aspectRatio);
                }
            });
            
            heightInput.addEventListener('input', function() {
                if (maintainRatio.checked && aspectRatio > 0) {
                    widthInput.value = Math.round(heightInput.value * aspectRatio);
                }
            });
            
            // Drag and drop functionality
            ['dragenter', 'dragover', 'dragleave', 'drop'].forEach(eventName => {
                dropArea.addEventListener(eventName, preventDefaults, false);
            });
            
            function preventDefaults(e) {
                e.preventDefault();
                e.stopPropagation();
            }
            
            ['dragenter', 'dragover'].forEach(eventName => {
                dropArea.addEventListener(eventName, highlight, false);
            });
            
            ['dragleave', 'drop'].forEach(eventName => {
                dropArea.addEventListener(eventName, unhighlight, false);
            });
            
            function highlight() {
                dropArea.style.borderColor = '#2575fc';
                dropArea.style.backgroundColor = '#eef1ff';
            }
            
            function unhighlight() {
                dropArea.style.borderColor = '#6a11cb';
                dropArea.style.backgroundColor = '#f8f9ff';
            }
            
            dropArea.addEventListener('drop', handleDrop, false);
            
            function handleDrop(e) {
                const dt = e.dataTransfer;
                const files = dt.files;
                handleFiles(files);
            }
            
            function handleFileSelect(e) {
                const files = e.target.files;
                handleFiles(files);
            }
            
            function handleFiles(files) {
                if (files.length === 0) return;
                
                const file = files[0];
                if (!file.type.match('image.*')) {
                    alert('Please select an image file (JPEG, PNG, etc.).');
                    return;
                }
                
                const reader = new FileReader();
                reader.onload = function(e) {
                    originalImage = new Image();
                    originalImage.onload = function() {
                        // Display original image
                        originalPreview.src = e.target.result;
                        
                        // Calculate aspect ratio
                        aspectRatio = originalImage.width / originalImage.height;
                        
                        // Set default dimensions
                        widthInput.value = originalImage.width;
                        heightInput.value = originalImage.height;
                        
                        // Enable inputs
                        widthInput.disabled = false;
                        heightInput.disabled = false;
                        
                        // Show original file info
                        const fileSizeKB = Math.round(file.size / 1024);
                        originalInfo.textContent = `Size: ${fileSizeKB} KB | Dimensions: ${originalImage.width}x${originalImage.height}`;
                        
                        // Enable resize button
                        resizeBtn.disabled = false;
                    };
                    originalImage.src = e.target.result;
                };
                reader.readAsDataURL(file);
            }
            
            // Resize image
            resizeBtn.addEventListener('click', function() {
                if (!originalImage) return;
                
                const width = parseInt(widthInput.value);
                const height = parseInt(heightInput.value);
                const quality = compressionSlider.value / 100;
                
                if (width <= 0 || height <= 0) {
                    alert('Please enter valid dimensions (greater than 0).');
                    return;
                }
                
                const canvas = document.createElement('canvas');
                const ctx = canvas.getContext('2d');
                
                // Set canvas dimensions
                canvas.width = width;
                canvas.height = height;
                
                // Draw image on canvas with new dimensions
                ctx.drawImage(originalImage, 0, 0, width, height);
                
                // Show loading state
                resizedPreview.src = "https://via.placeholder.com/400x300?text=Resizing...";
                resizedInfo.textContent = "Resizing...";
                
                // Convert to blob with specified quality
                canvas.toBlob(function(blob) {
                    if (!blob) {
                        alert("Resizing failed. Please try again with a different image.");
                        return;
                    }
                    
                    // Create URL for the resized image
                    const url = URL.createObjectURL(blob);
                    
                    // Display resized image
                    resizedPreview.src = url;
                    
                    // Show resized file info
                    const fileSizeKB = Math.round(blob.size / 1024);
                    resizedInfo.textContent = `Size: ${fileSizeKB} KB | Dimensions: ${width}x${height} | Quality: ${compressionSlider.value}%`;
                    
                    // Store resized image blob for download
                    resizedImageBlob = blob;
                    
                    // Show download button
                    downloadBtn.style.display = 'block';
                }, 'image/jpeg', quality);
            });
            
            // Download resized image
            downloadBtn.addEventListener('click', function() {
                if (!resizedImageBlob) return;
                
                const link = document.createElement('a');
                link.href = URL.createObjectURL(resizedImageBlob);
                link.download = 'resized-image.jpg';
                document.body.appendChild(link);
                link.click();
                document.body.removeChild(link);
            });
        });
    </script>
    
    <!-- Google AdSense Code (replace with your own Ad Unit IDs) -->
    <!--
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-app-pub-6877941501591932/6977620972" crossorigin="anonymous"></script>
    <script>
        (adsbygoogle = window.adsbygoogle || []).push({});
    </script>
    -->
</body>
</html>
